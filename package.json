{
  "name": "js-1sat-ord",
  "version": "0.0.12",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf .build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "SET NODE_OPTIONS=--openssl-legacy-provider&&cd node_modules/@ts-bitcoin/core && npm run build:node"
  },
  "keywords": [],
  "author": "Luke Rohenaz",
  "license": "MIT",
  "dependencies": {
    "bmapjs": "^0.4.0-beta.21",
    "bsv-wasm": "^2.0.0",
    "buffer": "^6.0.3",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "rimraf": "^4.4.0",
    "typescript": "^4.9.5"
  }
}